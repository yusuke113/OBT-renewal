import React from 'react';
import styles from './Blog.module.scss';

export const Blog = () => {
  return (
    <div className={styles.blog}>
      <h1>BLOG</h1>
    </div>
  );
};

export default Blog;
